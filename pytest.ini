[pytest]
; legacy/ holds the quarantined previous-generation pipeline (see legacy/README.md)
; and is never collected. The rest mirrors pytest's default norecursedirs.
norecursedirs = legacy *.egg .* _darcs build CVS dist node_modules venv {arch}
