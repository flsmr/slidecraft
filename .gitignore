# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# User workspaces (never commit user data)
# These are created by /slidecraft:start in user-chosen directories,
# not typically inside this repo, but just in case:
*.cif.json
.slide-creator.json

# Build outputs
dist/
*.pdf
*.png

# Environment
.env
.env.local

# Source PPTX templates (large binaries, kept in the deck repo's resources/)
IUG_PPT_Master_Update_EN_V01.pptx
temp.pptx
~$*.pptx

# Worktrees + agent transcripts
.claude/

# Skills CLI (npx skills add ...) — installs into .agents/ + maintains skills-lock.json
.agents/
skills-lock.json

# Transient render outputs
validation-output/
validation-output-generated/

# Old / abandoned working dirs
rebuild-proof/
rebuild-test/
pptx-reference-images/
pptx-reference-manifest.json

# Scratch artifacts from deck-generation test runs (may contain deck content — never commit)
*_prompt.txt
slide_plan.json
slide_plan_raw.txt
concept_prompt.txt
figure_content_prompt.txt
cards_prompt.txt

# Local-only ticket system (this machine only)
tickets/

# Local agent-skills config (machine-local conventions, not part of the public plugin)
CLAUDE.md
docs/agents/
